<views:MvxWindowsPage
    x:Class="Day0.Universal.Views.FirstView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Day0.Universal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.WindowsUWP.Views"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBox Text="{Binding FirstName, Mode=TwoWay}" FontSize="64"/>
            <TextBox Text="{Binding LastName, Mode=TwoWay}" FontSize="64"/>
            <TextBox Text="{Binding FullName, Mode=OneWay}" FontSize="64"/>
        </StackPanel>
    </Grid>
    
</views:MvxWindowsPage>

